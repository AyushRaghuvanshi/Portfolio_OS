// AyushOS content — About.
// Edit the text between the backticks below. It's the same simple markdown format
// as before; only the file wrapper changed so it loads by double-clicking the page
// (no web server needed). Avoid typing a backtick ` or the sequence ${ in the text.
window.DC_CONTENT = window.DC_CONTENT || {};
window.DC_CONTENT.about = String.raw`
# About — edit these fields, then reload the page.
# Format is "key: value". Long values (like bio) can wrap onto the next line.

name: Ayush Raghuwanshi
role: Founding Software Developer @ Benefi Global
avatar: AR
bio: I build the systems behind products — search engines, payment rails, notification
  pipelines and the cloud infrastructure they run on. Started in mobile with Flutter,
  now shipping across backend, iOS and AWS at a 20-service startup ecosystem.

# Comma-separated pills shown under the name.
tags: B.Tech IT · AKTU '25, Greater Noida, IN · Remote

# Leave "status" blank to hide the green "available" pill.
status: open to opportunities
`;

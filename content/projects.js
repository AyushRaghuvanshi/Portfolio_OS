// AyushOS content — Projects.
// Edit the text between the backticks below. Same markdown format as before.
// Avoid typing a backtick ` or the sequence ${ in the text.
window.DC_CONTENT = window.DC_CONTENT || {};
window.DC_CONTENT.projects = String.raw`
# Projects.
#
# Start each project with "## Project Name". Then optional fields:
#   subtitle:  @ Benefi Global         <- small grey text next to the title
#   desc:      one paragraph            <- can wrap onto following indented lines
#   tech:      OpenSearch, NestJS, ...  <- comma-separated, shown as a dotted list
#   link:      https://github.com/...   <- if set, the whole card becomes clickable
#   linklabel: github ↗                 <- optional label for the link (default "github ↗")

## Samriddhi Prawah
desc: Commodity-sharing platform for my college — Redis caching for 90% faster responses,
  QR-code security, CI/CD to AWS EC2 via GitHub Actions.
tech: Flutter, Node.js, Redis, EC2
link: https://github.com/AyushRaghuvanshi
linklabel: github ↗
`;
